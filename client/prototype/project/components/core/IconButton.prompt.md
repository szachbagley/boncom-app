**IconButton** — square, flat button holding a single icon; use for toolbar and table-row actions. Always pass `label` for accessibility.

```jsx
<IconButton label="Edit" variant="ghost"><i data-lucide="pencil"></i></IconButton>
```

Variants: `ghost` (default), `secondary` (outline), `accent` (cyan), `danger`. Sizes `sm | md | lg`.
