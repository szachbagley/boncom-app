**SectionLabel** — the spaced, uppercase micro-heading that opens a section. A signature Boncom device. Pair above a heading or over a group of fields.

```jsx
<SectionLabel>Project details</SectionLabel>
```

Props: `as` (element tag), `color`.
