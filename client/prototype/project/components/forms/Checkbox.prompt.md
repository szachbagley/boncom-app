**Checkbox** — square (never rounded) box; cyan fill + navy tick when checked. Works controlled or uncontrolled.

```jsx
<Checkbox label="Include contingency" defaultChecked />
```

Props: `label`, `checked`, `defaultChecked`, `disabled`, `onChange`.
