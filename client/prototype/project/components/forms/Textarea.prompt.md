**Textarea** — multi-line text field for notes and scope descriptions. Same border/focus treatment as Input.

```jsx
<Textarea label="Scope notes" rows={5} placeholder="Describe the deliverable…" />
```

Props: `label`, `hint`, `error`, `rows`, plus native textarea attributes.
