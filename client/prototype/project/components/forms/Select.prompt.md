**Select** — dropdown styled to match Input; navy chevron, sharp corners.

```jsx
<Select label="Status" options={[{value:'draft',label:'Draft'},{value:'sent',label:'Sent'}]} />
```

Props: `label`, `hint`, `error`, `options` (or `<option>` children).
